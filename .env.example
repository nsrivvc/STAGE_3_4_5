# --- Database connection (local/dummy Postgres for the prototype) ---
# Either set DATABASE_URL directly...
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/pipeline
# ...or leave it blank and set the parts below instead:
# PGUSER=postgres
# PGPASSWORD=postgres
# PGHOST=localhost
# PGPORT=5432
# PGDATABASE=pipeline

# --- Schemas (override if your raw/curated schemas are named differently) ---
BRONZE_SCHEMA=bronze
SILVER_SCHEMA=silver
# Where the decomposition phase lands its output tables. Stage 4's rec-del
# pairing reads from here, so this must match wherever that phase writes.
DECOMP_SCHEMA=silver_staging

# --- Parquet export ---
# Every table's rows are written here before the transaction commits.
# Set to "" (or pass --no-parquet) to turn the export off entirely.
PARQUET_OUTPUT_DIR=parquet_output
# Top-level folder for a run's files. The workflows set this per stage
# (stage3 / stage4 / stage5); left empty it falls back to the transformation's
# own folder (stage_3 / stage_4).
PARQUET_STAGE=

# --- Logging ---
LOG_LEVEL=INFO
