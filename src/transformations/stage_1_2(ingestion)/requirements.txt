# Core ingestion dependencies
psycopg[binary]>=3.1,<4.0      # Postgres / Neon driver (psycopg 3)
python-dotenv>=1.0,<2.0        # loads .env for local runs (optional at runtime)
pyarrow>=16.0,<25.0            # pre-load Parquet export (src/parquet_export.py)

# Mock NatGasHub API (src/mock_api) — local development only
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0

# --- Add later when migrating to Azure SQL ---
# pyodbc>=5.0,<6.0             # Azure SQL Database driver (needs MS ODBC Driver 18)
